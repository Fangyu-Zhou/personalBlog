spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/personalBlog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: lovemomo940219
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.rain.blog: debug
  file:  log/blog-dev.log


